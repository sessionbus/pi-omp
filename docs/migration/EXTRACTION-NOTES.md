# Pi/OMP extraction record

Baseline: `710e5d33369cba4fb9468cd24fea0fe844a0219d` from the combined peers repository. This destination is `sessionbus/pi-omp`, retaining both products and their shared `pifamily` code. The frozen inventory maps 105 protected files and 346 original Go tests: 71 Pi/OMP files with 277 tests, two shared packaging surfaces, and 32 shared-support files with 69 tests. The latter resolve from `peer-common` at `eb655f686e4456a4c1121054763318e3d27e89b0`, pinned as `v0.0.0-20260922143100-eb655f686e44`. There is no workspace or local module replacement.

Pi and OMP retain their command wrappers, managed and interactive paths, native extension source, manifests, installers, design and acceptance records. Go changes in protected product files only relocate imports to this module or pinned common support. The package script keeps the two existing extension-copy branches: Pi stages `pi/{extension,native}.mjs` plus `pifamily/extension/bridge.mjs`; OMP stages `omp/extension.mjs` plus the same bridge. The archive installer keeps their Go `--sessionbus-install --plugin-dir` maintenance path. The only canonical URL edits are in the two bootstrap installers. No npm package, Node build step, Node sidecar or peer MCP alias is introduced. Node 24 is needed in CI solely to run the four existing extension test files; the native Pi application already uses Node and OMP uses Bun.

The four extension test files retain 48 static test registrations and pass 50 runtime tests. Two product tests formerly read the closed MCP declaration under the removed OpenCode product tree. A byte-identical copy (SHA256 `bfe5acd1aa93734e66dbe6c79f58a4f2bd7779908103a017cf11c23266e55823`) now lives at `wrappers/pifamily/extension/testdata/sessionbus-tool.json`; only those two test fixture paths changed. It is not packaged or read by either runtime extension.

Release-note links to `docs/designs/pi-omp-0.5.0` remain local and resolve here. The root module, architecture/version checks, CI and archive build are scoped to Pi/OMP. Historical release notes record the original combined release and should not be read as publication of an independent Pi/OMP archive.

Pi and OMP remain **preview** integrations. The original acceptance record distinguishes Pi normal Run, delivery/Forget, interactive replacement and startup/resume from OMP zero-input Worker/interactive and core normal Run. Some fixture outcomes were assessed from retained evidence rather than full fixture passes. Pi admitted interrupt/recovery and OMP staged delivery/Forget/interactive context remain open; arbitrary stdout from an OMP native extension can invalidate its RPC connection. Source preservation does not close those limits.

The `binary-release` and preview-publication workflows remain held during migration. Before enabling Actions, inspect the destination repository's `develop` branch and disable the release workflow or retire that branch; it otherwise publishes on a develop push. No release or version change is claimed. The original historical evidence remains bound to its tested binaries. Native version/hash is observation provenance, not a compatibility restriction.

## Installed results on the extracted build

The reviewed `6a7379f` Linux archives replaced the prior real-home UMKA installations. Pi replaced source `b2330276` with binary `63a834c0`; OMP's prior installed binary was `71d8b4db`, but its before observation had no source revision, so that hash alone does not establish a prior source commit. `d84c4e1` is historical attribution, not a source field established by that before observation. OMP's extracted binary is `104c22d2`. Each product installer completed twice, its after-first/after-second product observation was identical, and the other product's installed observation stayed unchanged. The archive member hashes, aliases, native settings, and active Sessionbus service were checked independently in the external evidence packets `pi-omp-installed-dev1-20260923/BINDING-PI.json` and `BINDING-OMP.json` under `/home/antst/sessionbus-evidence`. These installations use the permanent login home/PATH/config, not a parallel prefix. Native Pi `0.85.1` and OMP `18.1.17` are recorded as provenance only.

Four fresh Pi wake surfaces passed their original launcher, driver, reviewed projector, phase guard, direct-reply and owned-cleanup gates: managed idle `PIW923C`, managed active `PIW923D`, interactive idle `PIW923E`, and interactive active `PIW923G`. The interactive setup was supplied once as argv `initial_prompt` with zero PTY writes; active cells retained the exact Bash sleep generation across the send and post-receipt window. One inbound message was sent per cell, with no replay or post-inbound native prompt. Raw reply notifications were operator-attested and joined to the native Sessionbus send result before the sole launcher continuation; they are not cryptographic receipts. The retained `PIW923A` identity collision and `PIW923B` stdin-EOF launcher failure do not count as clean passes. `PIW923F` had original live gates pass but its pre-registered strict projector failed on a missing Bash `timeout` field; the peer setup did not request a timeout, and a reviewed fixture-specific checker later qualified F retrospectively. F remains **not** a clean original full pass; G is the fresh clean interactive-active pass.

Fresh OMP managed-idle cell `OMW923A` is a **clean original pass**, with a qualification: its spawn explicitly used `idle_message: run` as the reviewed OMP adapter requires. The omitted/default idle policy was not exercised. The original driver exited 0 after one `injected` receipt, distinct setup and automatic wake Runs, a native-joined direct reply and owned cleanup; the designated OMP projector and phase guard ran inside it before completion. An independent raw review confirmed the native inventory and joins. A separate operator invocation of the bare shared guard rejected `idle_message: run`; that diagnostic is retained and was the wrong gate for OMP. OMP managed active and interactive idle/active remain **untested and held** for lack of OMP-specific harness coverage; they have not failed and are not inferred from Pi.

The Pi cells used the reviewed `f37f86ca` adapter, with `PIW923G` using its fixture-specific `0ab47768` checker successor. OMP used adapter `77b11d3d`; both used the reviewed `9327c91d` v6.2 launcher for their final cells. Its recorded residual risks are a FIFO at the operator reply-file path blocking the read, and a grandchild escaping owned process-group cleanup through `setsid`. The cells used the regular-file publisher and retained independent process scans. Both products remain **preview**; release and preview-publication workflows remain disabled, with no tag, release or version change. The older open historical limits above remain separate from these installed observations.
