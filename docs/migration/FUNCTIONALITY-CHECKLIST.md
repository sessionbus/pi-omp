# Pi/OMP functionality preservation

Source baseline: combined peers `710e5d33369cba4fb9468cd24fea0fe844a0219d`. Frozen mapping: `PRESERVED-FILES.json`. Source preservation and fresh installed acceptance are separate.

| Contract | Source and test preservation | Installed evidence / remaining limit |
|---|---|---|
| Pi and OMP command dispatch, native argv, permissions and normal process exit | Both `cmd/*-peer` and `wrappers/{pi,omp}` with original Go tests | Permanent `6a7379f` Pi and OMP builds installed independently in the real home; native Pi `0.85.1` and OMP `18.1.17` are provenance only; no permission broadening |
| Managed Run/Worker lifecycle, readiness, result, cancellation and cleanup | Product tests plus pinned `peer-common` host/socket/version tests | Pi managed idle `PIW923C` and active `PIW923D` are clean original wake passes. OMP managed idle `OMW923A` is an independently reviewed clean original pass with explicit `idle_message: run`; omitted/default idle policy and OMP managed active remain untested |
| Interactive launch, reconnect, delivery and Forget | Product and `pifamily` tests | Pi interactive idle `PIW923E` and active `PIW923G` are clean original wake passes; one argv initial prompt, zero PTY writes, no replay. OMP interactive idle/active remain untested; historical staged context and Pi interrupt recovery remain preview work |
| Native extension bridge and exact closed MCP declaration | Four original `.test.mjs` files, 48 static registrations and 50 passing runtime tests; byte-exact shared declaration test fixture copied locally | Pi uses its existing Node host and OMP its Bun host; fresh replies are operator-attested raw notifications joined to native send results, not cryptographic receipts |
| Archive build, plugin members, role and checksum-safe bootstrap | `scripts/package-product`, `scripts/release/install-product`, two bootstrap scripts and boundary/download tests | Both exact real-home archives installed and reinstalled idempotently; Pi before source `b2330276`, OMP before source absent with prior binary `71d8b4db`; installed `6a7379f` product observations and other-product isolation independently checked |
| Module and source boundary | 105 frozen files, 346 Go tests; 32 common files at pinned `eb655f6`; root architecture/version tests | No product-path rewrite or alternate install prefix |
| Preview and release status | Local Pi/OMP design/acceptance/release-note links; held binary-release workflow | Both products stay preview. No publication before release gate review. |

The frozen inventory contains 71 product files, two shared packaging surfaces and 32 pinned common files. Product Go changes are import relocation only. The only exception in test assets is the local, byte-identical schema fixture and two path references described in `EXTRACTION-NOTES.md`. Original source tests are distinct from the installed cell results. Pi `PIW923A/B` remain harness failures; `PIW923F` is retrospectively qualified under a reviewed fixture-specific timeout checker but is not a clean original pass. The separate `PIW923G` supplies clean interactive-active evidence. The OMP cell used explicit `idle_message: run`; omitted/default idle policy and the other OMP surfaces are not accepted by that observation. The reviewed v6.2 launcher records FIFO and escaped-`setsid` grandchild cleanup residual risks; final cells used its regular-file publisher and independent process scans. Pi and OMP remain preview, with release and preview-publication held.
